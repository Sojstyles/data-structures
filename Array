    
# ARRAY
        
array = []
# We use a list in Python instead

#------------------------------------------------------#

# Examples:

array += [num for num in range(10) if num % 2 == 0]
print(array)

array *= 2
print(array)

print(array[::3])


